This app is not meant for production use. Currently changes made to the database will destroy all old data.

Changing to map view will crash the app if service is not available.

To build the app in Eclipse, create a new Android project from sources.
